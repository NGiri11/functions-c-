Code to calculate percentage and sum:-

#include <iostream>

using namespace std;

int main()
{
    int per,sum=0,m,p,c,e,cs;
    cout<<"enter your marks of maths,physics,chemistry,english,computer science";
    cin>>m>>p>>c>>e>>cs;
    sum=sum+m+p+c+e+cs;
    per=(sum)/5;
    cout<<"sum and percentage is "<<sum<<endl<<per;

    return 0;
}

Code to calculate simple interest and compound interest:-

#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    float si,ci,n,a,p,r,t;
    cout<<"Enter the principal amount, rate of interest, time period and quarter";
    cin>>p>>r>>t>>n;
    si=(p*r*t)/100;
    a=p*pow((1+(r/(n*100))),(n*t));
    ci=a-p;
    cout<<"simple interest is ="<<si<<endl;
    cout<<"compound interest is ="<<ci;

    return 0;
}

Code to calculate area and circumference of circle:-

#include <iostream>
using namespace std;

int main()
{
    float circum,area,rad;
    cout<<"Enter the radius";
    cin>>rad;
    area=rad*rad*(3.14);
    circum=2*(3.14)*rad;
    cout<<"Area is ="<<area<<endl;
    cout<<"Circumference is ="<<circum;

    return 0;
}
